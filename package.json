{
  "name": "large-package",
  "version": "1.0.0",
  "description": "A package with 2GB of dependencies", 
  "dependencies": {

// Generates 1.9 GB of dependencies 
// Each dependency points to a 1MB module

{% for i in range(1900) %}
    "module-{{i}}": "1.0.0",
{% endfor %}


// Additional 100MB to reach 2GB
    "final-module": "1.0.0"
  }
}

